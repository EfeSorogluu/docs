---
title: 'AuthMe Entegrasyonu'
description: 'Bu rehber, Minecraft sunucunuzda AuthMe eklentisini Crafter CMS ile entegre etmek için gerekli adımları detaylıca açıklar.'
---

import { Steps, Callout } from 'nextra/components'

## Adım 1: MySQL Sunucusu Kurulumu

Öncelikle, Minecraft sunucunuzun çalıştığı makineye bir MySQL sunucusu kurmanız gerekmektedir. Ayrıca, güvenlik duvarında (firewall) MySQL portunu (genellikle 3306) açmalısınız.

<CodeGroup>
```powershell Windows (PowerShell)
# Chocolatey ile MySQL Kurulumu
choco install mysql

# MySQL servisinin başlatılması
Start-Service mysql

# Gerekirse firewall'da 3306 portunu açın
New-NetFirewallRule -DisplayName "MySQL Port" -Direction Inbound -LocalPort 3306 -Protocol TCP -Action Allow
```

```bash Linux (Ubuntu)
# MySQL Server'ı yükleyin
sudo apt update
sudo apt install mysql-server

# MySQL servisinin başlatılması
sudo systemctl start mysql

# Gerekirse firewall'da 3306 portunu açın
sudo ufw allow 3306/tcp
```
</CodeGroup>


## Adım 2: MySQL Kullanıcınızı yapılandırın.

Öncelikle bir CMD, Powershell veya terminal açın ve aşağıdaki komutu çalıştırın.

<CodeBlock language="bash">
mysql -u root
</CodeBlock>

Girdiğiniz komutu çalıştırdıktan sonra MySQL'e bağlanacaksınız, aşağıdaki komutları sırasıyla çalıştırın.

```sql
CREATE USER 'authme'@'localhost' IDENTIFIED BY 'burayagüvenlişifrenizigirin';
CREATE USER 'authme'@'*' IDENTIFIED BY 'burayagüvenlişifrenizigirin';
GRANT ALL PRIVILEGES ON authme.* TO 'authme'@'localhost';
GRANT ALL PRIVILEGES ON authme.* TO 'authme'@'*';
FLUSH PRIVILEGES;
```

Bağlantı testi yapın, yeni bir komut satırı açın ve aşağıdaki komutu çalıştırın.

```mysql
mysql -u authme -p

Password: ....
```

Eğer bağlantı başarılıysa, sonraki adımlara ilerleyebilirsiniz.

## Adım 3: AuthMe ile MySQL Bağlantısı

AuthMe'nin MySQL veritabanına bağlanabilmesi için, eklentinin konfigürasyon dosyasını (örneğin, `config.yml`) örnek dosya ile değiştirmelisiniz. Bu dosyada veritabanı bağlantı bilgileri doğru şekilde girilmiş olmalıdır.

<Callout type="info">Kendi hazırladığınız <code>config.yml</code> dosyasını AuthMe'nin kurulu olduğu dizine kopyalayın ve eski dosyanın üzerine yazın.</Callout>

## Örnek AuthMe config.yml Dosyası

Aşağıda, AuthMe eklentisinin MySQL bağlantısı için kullanabileceğiniz örnek bir `config.yml` yapılandırması yer almaktadır. 

<Callout type="warning">
<strong>Önemli:</strong> <code>mySQLTablename</code> değeri, Crafter CMS panelinde AuthMe ayarlarında belirttiğiniz tablo ismiyle <b>birebir aynı</b> olmalıdır. Aksi halde entegrasyon çalışmaz.
</Callout>


```yaml config.yml
DataSource:
    backend: MYSQL
    caching: true
    mySQLHost: 127.0.0.1
    mySQLPort: '3306'
    mySQLUseSSL: false
    mySQLCheckServerCertificate: true
    mySQLAllowPublicKeyRetrieval: true
    mySQLUsername: authme
    mySQLPassword: 'burayagüvenlişifrenizigirin'
    mySQLDatabase: authme
    mySQLTablename: users # Crafter'a verilecek tablo ismi
    ....
    # Aşağıdaki alanları AuthMe config.yml dosyanızda bulun.
    passwordHash: BCRYPT
    legacyHashes: ['SHA256'] # Eğer ki önceden farklı bir hash algoritması kullanıyorsanız, bu alanı kullandığınız hash algoritmasına göre düzenleyin.
```

## Adım 4: Crafter CMS Yönetim Paneline Giriş

[Crafter Kontrol Paneli](https://crafter.net.tr/dashboard)'ne gidin ve kullanıcı bilgilerinizle giriş yapın.

## Adım 5: Resimlerdeki yönlendirmeleri takip edin.

Yan menüde sunucu ikonunu seçin.
<img src="/images/select-servers.png" alt="AuthMe Entegrasyonu Aşama 1" />
Açılan menüden <b>AuthMe</b> ayarlarını seçin.
<img src="/images/authme/select-authme.png" alt="AuthMe Entegrasyonu Aşama 2" />
Bilgileri doldurduktan sonra <b>Kaydet</b> butonuna tıklayın.
<img src="/images/authme/send-form.png" alt="AuthMe Entegrasyonu Aşama 3" />

<Info>Eğer ki bu aşamada çok uzun süre bekliyor veya hata alıyorsanız, yapılandırma sırasında bir hata oluşmuş demektir.</Info>

---

Kurulum tamamlanmıştır! Artık Minecraft sunucunuzdaki AuthMe verileri Crafter CMS ile entegre şekilde çalışacaktır.